API_BASE_URL=http://127.0.0.1:4000
# Server-side workplace for PIN login and worker BFF routes (from pnpm seed:demo).
WORKER_COMPANY_ID=
# Deprecated: browser must not supply company ID. Use WORKER_COMPANY_ID only.
NEXT_PUBLIC_WORKER_COMPANY_ID=
# Server-side location credentials for clock punches (from pnpm seed:demo).
KIOSK_ID=
KIOSK_SECRET=
# Signs Field employee session cookies (httpOnly). Falls back to KIOSK_SECRET or WORKER_COMPANY_ID locally.
FIELD_SESSION_SECRET=
